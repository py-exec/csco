from django.apps import AppConfig

class CalculatorPriceBoxConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.calculator_price_box'  # مسیر صحیح را بررسی کنید